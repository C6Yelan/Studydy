# 單一觀念重點題組（B5-D）

使用者已確認：一次產生一組題目，合起來涵蓋**一個觀念**的多個重點。題數依該觀念可由來源出題的不同重點決定，不固定五題，也不跨章節抽選多個觀念。

## B05 階段狀態（2026-09-20）

B5-Q 出題檢查與候選排序、B5-D 題組及 B5-R 錯題補強均已實作並載入日常服務，功能驗證紀錄見下文及對應文件。當時資料庫使用舊 migration 鏈；2026-09-23 才接軌目前的四份領域 baseline（0001～0004）。整組一次交卷，不同觀念可各自保留未完成題組。開發模型實跑通過限於記錄的 Luna 案例，不代表一般教材品質或正式 Gemma 品質驗收。

待改善項目包括題目重要性、答案線索與干擾選項，以及計畫階段未合併語意重疊重點，造成出題時被判重複而生成不足。依使用者決定，整體 UI／UX 留待後續集中整理，阻擋使用或影響資料保存的問題仍需即時修正。這些限制不阻擋進入 B06 整體流程驗收，也不能在後續驗收中省略或冒稱已解決。

## 出題與作答

先讀取選定 Concept 的計畫，不呼叫模型；只排除沒有引用內容的 Claims。上游可能把完整定義、儲存假設標成 heading，因此不以版面類型直接排除，仍交由來源與唯一答案檢查確認題目。同一份 Evidence、相同文字的重點合併，保留大小寫、數值與條件，不用額外模型猜測重點是否等價。保存原定題數、順序、主要 Claim、共同涵蓋的 Claims、排除理由及執行設定。

一次操作建立整組。現有 worker 串行準備各題，每題沿用 B5-Q 的三個候選與一次盲解檢查，通常是兩次模型呼叫。整組操作不等於一次模型 HTTP 呼叫。已驗證的題目先私存，完整備妥才一次發布；品質排序沒有最低分，合法的基礎題不會因干擾項較弱而全部被拒絕。

若部分題目失敗，可以只重試未備妥的重點，或明確選擇「先做已備妥的題目」。同一重點最多兩次準備，不自動重播模型請求；已備妥的題目不重做。選擇部分發布後，題數與成員即封存，原定而未發布的重點仍顯示未檢測，不事後縮小原定目標。

發布後一次看到整組，**選完所有已發布題目再一次交卷**。交卷前可修改選項，單純選取不算保存，也不逐題顯示正誤。伺服器在同一交易驗證並保存整組答案，任一項失敗全部回滾；成功時同時結束本組、顯示完整回饋與補強建議，不再另按結束初篩／補強。回應遺失可查回整組結果或以相同意圖重送，不重複計分。已完成或取消的題組可回顧，重新整理與重新登入不重新出題。

題組只接受整組交卷；獨立單題生成、讀取、提交與舊 guidance API 已移除，不再提供未分組單題的相容讀取。已保存的資料不改寫。已發布但省略的重點維持未檢測，整組交卷只要求作答實際發布的題目。

摘要顯示本次通過、待補強、尚未作答與未檢測。**本輪通過不代表已掌握**；原有學習 reducer 仍要求每個重點兩道不同的合格正確題且最新作答正確。未出題、生成失敗或未答不算學生答錯。本批完成題組功能；錯題導向的新補強輪已由 [B5-R](assessment-remediation.md) 接上；補強只計入本次改善，不累計為獨立掌握證據，原答案與資格不改寫。

## 保存與併發邊界

migration `0004_assessment_sets.sql` 建立 `assessment_sets`／`assessment_set_items`，不重寫既有教材、題目、答案或 hashes。不同觀念可各自保留進行中的題組；同一 StudySession／Concept 不重複建立進行中的題組。資料庫唯一限制、完整 scope 外鍵及發布不可變 trigger 保護成員關係。前後端僅使用觀念題組入口；沒有單題歷史路由或舊 guidance 元件／API。

預留與提交使用短交易，模型呼叫在交易外。逐題 lease／token 配合心跳防止晚到結果發布，過期工作記為中斷而非自動再呼叫模型。已驗證結果遇到不確定的 DB commit 最多補存一次相同結果，不重新出題。取消或刪除教材會失效化工作；實際刪除時先清理題組成員，再清理原有題目與學習記錄。

作答權限以伺服器的已發布題組成員為準，不能靠前端傳入 Claim 或取消所有 current-focus 檢查。公開讀取不包含執行設定、原始候選、私有正解或未發布的題目。

學生介面不提供取消題組入口；僅保留 worker 對已終止 StudySession 的 internal cancellation。等待內容是同一 AssessmentSetPanel 的 preparing 狀態，沒有額外 preparation route。

## API

| 用途 | 路徑 |
|---|---|
| 讀取單一觀念計畫 | `GET /v1/study-sessions/{sid}/assessment-plan?concept_id=...` |
| 建立／列出題組 | `POST / GET /v1/study-sessions/{sid}/assessment-sets` |
| 讀取同一題組 | `GET /v1/study-sessions/{sid}/assessment-sets/{set_id}` |
| 整組交卷 | `POST .../assessment-sets/{set_id}/submissions`，含全部已發布題目選項、版本與操作意圖 |
| 明確重試／部分發布 | `POST .../{set_id}/{retry|publish-partial}` |

建立帶 `target_concept_id` 與 Idempotency-Key，不接受前端任填題數；修改帶版本與 Idempotency-Key。現行 `study-resume/v1` 只包含題組摘要與選取 ID，不再投影 `assessments`／`selected_assessment_revision`，URL 可指向原題組。讀取使用一致 snapshot，過期讀取不覆蓋較新版本；明確版本衝突讀回最新狀態，未知結果保留原操作意圖。

## 驗證與實際限制（2026-09-20）

- 隔離 PostgreSQL／API 測試覆蓋 1、3、7 題，同一觀念範圍、私有準備、部分發布、只重試失敗題、並行建立、不可變成員、過期／取消／刪除及 commit 回應不確定；推論期間另一 DB connection 可立即取得 session 鎖。
- 真 API／DB browser 在 1366 與 390 寬度驗證整組準備、reload、三題獨立選取、逐題保存、遺失回應查回、完成後新登入恢復，使用受控模型，沒有外部推論。
- 題組／既有保存題目的恢復與教材庫回歸合計 17 passed。完整後端首次 340 passed／1 failed；唯一失敗為地圖導覽預設收合後的舊 browser assertion，修正為先展開後，包含在上述通過回歸。前端型別、build 與 Node 測試通過（API client 26 個案例另直接執行確認）。既有 mock browser 107 passed／2 failed；兩項仍呼叫已移除的單題生成按鈕，改為直接準備既有保存題後，相關 10 項回歸全部通過。

使用者另授權合理追加 Luna 呼叫。實際以自製 Stack 合成教材、受控已保存 KS，在隔離 DB 走完整題組 API／worker：LIFO、push、pop 三個重點各一題，**6 次呼叫、0 次重試、3／3 發布，約 112 秒**；再以真私有答案提交兩題正確、一題錯誤，驗證原答案重播及完成後恢復。沒有私人教材、Pod、產品 DB 寫入或額外 OCR。

逐題對照來源，三題都有唯一正解，涵蓋三個不同重點。LIFO 題的干擾項偏弱；pop 題混入 LIFO／FIFO 原則，選項類型不完全一致，checker 沒有提示此問題。這是基礎辨識題的單輪開發驗證，不能據此宣稱高階應用題品質、一般教材成功率、上游 ingestion 穩定或正式 Gemma 驗收。原始呼叫、公開／私有題目與審查紀錄留在私人 `../.studydy-product/experiments/b05d-round-20260920/`。CLI 未提供可解析的 token usage，不填造數字。


## 私人教材實跑與日常切換

使用者於同日明確批准私人教材呼叫模型。沿用當時既有觀念的六題計畫，使用日常前端／API／worker 與 Luna 真實出題：12 次呼叫、零重試、6／6 發布，276.18 秒。六題皆有對應來源支持的唯一答案；沒有代替使用者作答，AnswerEvent、原教材、Evidence／KS 與 PDF 記錄核對不變。服務重啟及 reload 後仍是相同六題，沒有再次推論。

實跑發現上游把完整的陣列儲存假設標成 heading，原始計畫因而漏選。已移除這種依版面分類的排除，補上實際 heading 來源的隔離回歸（題組 14 passed）；日常唯讀計畫現在包含七個重點。原六題計畫與成員保持封存，漏選的那項仍是本輪未檢測，沒有重新產生其餘六題或擅自加題。

這輪功能為 **FLOW_PASS**，題目品質為 **NEEDS_REVIEW**：有以條列編號直接提示答案的題目，也有只問子任務數量、函式名稱的題目。checker 未完整標出這些問題。對應不同 Claims 不代表已檢測各 Claim 內所有事實，不能把這批題組當成完整理解或掌握驗收。來源中的字型編碼數學符號也仍照原文保留。本次沒有為提高測試分數重跑已發布題目；原文、候選／答案與逐題審查僅保存於私人 `../.studydy-product/experiments/b05d-private-round-20260920/`。

日常服務已透過 `manage.py stop/start` 載入 B5-D 與 heading 修正，入口 `http://127.0.0.1:4173`。DB 升級前已備份，migration 0011 升級時 13 張既有表的原有欄位與 migration checksums 全部相同。原登入狀態已實際驗證教材庫、地圖、舊題目、題組範圍與六題恢復，console 無錯誤；登入活動僅更新 session 的閒置期限與活動時間。私人備份與逐表核對在 `../.studydy-product/experiments/b05d-cutover-20260920/`。


## 整組交卷與測驗版面更正（2026-09-20）

依使用者最新要求，原先逐題送出的操作已改為單一交卷按鈕。整組輸入先驗證完整成員、題目／選項與原有作答，再用現有評分核心於同一 DB 交易保存 AnswerEvents、操作回執與本組結束狀態。少答、重複項、錯誤選項、其他題組混入、版本衝突及中途保存失敗都不留下半份作答。同一意圖重播仍回原結果，另一個提交意圖不能改答案重刷分。

題卡統一單欄與寬度，桌機每題的選項兩欄排列，手機改單欄；未發布項目移到範圍說明，不留空題卡。交卷前隱藏結果／補強區，交卷後保持相同主欄寬度，教材內容可收合，桌機資訊側欄隨捲動保留位置。表單內來源對話框的關閉按鈕不會觸發交卷。

既有四題案例中，第 2 個目標兩次產生的共六個候選都被檢查器判為與既有題相同的考點，故保留三題，不是模型未啟動或儲存失敗。此處是重疊重點未於計畫階段合併的已知問題；本次交卷與 UI 更正沒有改寫其原計畫或為它重跑模型。


此次整組交卷修正的驗證：交易／題組／補強 29 passed；既有作答、接續、引導 34 passed；另外兩個刪除案例第一次因跨目錄未載入 fixture 而未執行，按 runtime 範圍重測後 2 passed。真 API／DB browser 7 passed，四種題組寬度及補強／舊題恢復皆通過；最後版面另測桌機／手機 2 passed、完整交卷回應檢查另測 1 passed。既有頁框／歷史／縮放 browser 21 passed，前端 Node 47 passed，TypeScript／build 通過。

已重新載入日常服務；此修正不新增 migration，schema 維持 0012，沒有模型呼叫。切換前後逐表核對原資料一致，僅正常登入活動時間可更新。實際登入讀取原題組確認三張等寬題卡、一個整組交卷按鈕、零個逐題送出按鈕、無水平溢出或 console 錯誤。切換前使用者已保存的三筆作答保留，本次沒有代交卷。私人核對資料位於 `../.studydy-product/experiments/b05-batch-submit-20260920/`。


## 觀念切換與未完成題組（2026-09-20 更正）

使用者明確指出：未完成 A 不應阻止進入 B。原本的全 StudySession 單一進行中題組限制範圍過大，現已移除切換焦點的封鎖；migration 0013 把唯一索引改為 `(study_session_id, target_concept_id)`。A 可以繼續準備／保留未答題，B 可以瀏覽、建立及作答自己的題組，回到 A 接續原題。

題組作答依原 owner、KS、Concept 與不可變成員驗證，不依目前導覽焦點授權。前端 resume 的檢查也按同一規則處理。題組頁 URL 固定指向實際題組，另一視窗切換焦點不改掉本頁內容。重試及補強也只受同一觀念中的進行中題組限制。

題組列表改為 `assessment-set-list/v1`，`active_set_ids` 如實列出多個觀念各自的進行中題組；同一觀念重複建立回 `ASSESSMENT_SET_ACTIVE`，前端接續該觀念原題組，不重跑模型。版本衝突另用原 `ASSESSMENT_SET_CONFLICT`：明確拒絕的交卷會讀回最新狀態、保留選項，下一次確認用最新版本送出；網路結果未知仍沿用原提交意圖查回，不冒然換 key 重送。


跨觀念修正驗證：題組／交卷／補強／切換核心 31 passed，既有閉環、恢復與 migration 回歸 34 passed；桌機與手機真 API／DB 的 A → B → A 接續 2 passed，另有版本衝突後重送與補強 browser 2 passed；前端 Node 49 passed，TypeScript／build 通過。首次 browser 找到前端 resume 還以目前焦點拒絕其他觀念的已發布題目，已改為依題組成員驗證後通過；未分組單題的範圍檢查保留。

已備份持久 DB 並套用 migration 0013，15 張既有表的原有欄位與 migration checksums 核對不變；日常前後端已重新載入。題組列表 v3 與原有教材／題組讀取正常，console 無錯誤。這次沒有取消、交卷、重建產品題組或呼叫模型。私人切換證據位於 `../.studydy-product/experiments/b05-concept-scope-20260920/`。
